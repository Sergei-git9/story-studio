# 🎬 Story Studio - Техническая документация

## 📋 Обзор проекта

**Story Studio** - это интерактивная платформа для создания кинематографических историй в формате чата с автоматическими визуальными эффектами и возможностью записи экрана для TikTok контента.

## 🏗️ Архитектура проекта

### 📁 Структура файлов
```
story-studio/
├── index.html                    # Главная страница выбора историй
├── recording-auto.html           # Плеер с автозаписью для TikTok
├── stories/
│   └── zakolka/                  # Пример истории "Заколка"
│       ├── ep1.json - ep5.json   # Эпизоды истории
│       └── effects-triggers.js   # 🔥 СИСТЕМА ТРИГГЕРОВ ЭФФЕКТОВ
├── scripts/
│   ├── utils.js                  # Утилиты (loadJSON, sleep, getUrlParams)
│   └── player-auto-simple.js     # 🎮 ОСНОВНОЙ ДВИЖОК ПЛЕЕРА
├── style/
│   ├── main.css                  # Основные стили
│   ├── chat.css                  # Стили чата
│   ├── animations.css            # Анимации
│   └── effects.css               # 🎨 CSS ЭФФЕКТЫ И АНИМАЦИИ
└── assets/sounds/                # Звуковые эффекты
```

## 🔥 Система триггеров эффектов

### 📍 Ключевой файл: `stories/zakolka/effects-triggers.js`

Это **сердце системы** - умный анализатор текста, который автоматически активирует визуальные эффекты.

#### 🎯 Как работает:
1. **Анализ текста** - сканирует каждое сообщение на ключевые слова
2. **Категоризация** - определяет тип эффекта (огонь, холод, мистика)
3. **Прогрессивная интенсивность** - усиливает эффекты при повторении
4. **Комбо-эффекты** - активирует мощные эффекты при совпадении нескольких триггеров

#### 🔧 Категории триггеров:

```javascript
// 🔥 ОГОНЬ (прогрессивные эффекты 1-5 уровней)
fire: {
  keywords: ['огонь', 'жжет', 'обжигает', 'раскален', 'пламя'],
  effects: ['fire_glow', 'screen_heat', 'burn_flash'],
  intensity: 'progressive' // Усиливается с каждым упоминанием
}

// ⚡ ЭЛЕКТРИЧЕСТВО
electrical: {
  keywords: ['трансформатор', 'искры', 'замыкание'],
  effects: ['electric_spark', 'screen_flicker']
}

// 👻 МИСТИКА
supernatural: {
  keywords: ['шепот', 'призрак', 'Observer', 'артефакт'],
  effects: ['ghost_flicker', 'static_noise']
}
```

### 🎮 Интеграция в плеер: `scripts/player-auto-simple.js`

#### Метод `checkForEffects(text)`:
```javascript
// Использует систему триггеров если доступна
if (typeof triggerEffects === 'function') {
    const effects = triggerEffects(text);
    effects.forEach(effect => {
        this.activateEffect(effect); // Активирует визуальные эффекты
    });
}
```

#### Метод `activateEffect(effect)`:
Преобразует абстрактные эффекты в конкретные визуальные элементы:
- `fire_glow` → красное свечение экрана
- `screen_shake` → тряска при высокой интенсивности
- `ghost_flicker` → мерцание призрака

## 🎨 Визуальные эффекты: `style/effects.css`

### Ключевые анимации:
```css
@keyframes fireGlow {
    0% { opacity: 0; transform: scale(0.8); }
    50% { opacity: 1; transform: scale(1.2); }
    100% { opacity: 0; transform: scale(1); }
}

@keyframes screenShake {
    0%, 100% { transform: translate(0); }
    10% { transform: translate(-2px, -1px); }
    /* ... тряска экрана */
}
```

## 📱 Режим записи: `recording-auto.html`

### Особенности:
- **Автоматический старт** с 10-секундным отсчетом
- **Скрытие UI** во время записи
- **Оптимизация для TikTok** (вертикальный формат)
- **Встроенная запись экрана** через Web API

## 📊 Формат данных историй

### Структура JSON эпизода:
```json
[
  {
    "type": "system",           // Тип: system/msg
    "text": "Заколка жжет ладонь", // Текст (анализируется триггерами)
    "sound": "system",          // Звуковой эффект
    "delay": 2500              // Задержка перед следующим сообщением
  },
  {
    "type": "msg",
    "side": "left",            // Сторона: left/right
    "name": "Катя",            // Имя отправителя
    "time": "21:02",           // Время сообщения
    "text": "Тём, ты где?",      // Текст сообщения
    "sound": "click",          // Звук уведомления
    "typing": true             // Показать анимацию печати
  }
]
```

## 🔍 Для ИИ: Ключевые файлы для понимания

Если ИИ хочет понять, как работает проект:

1. **`effects-triggers.js`** - система умных триггеров эффектов
2. **`player-auto-simple.js`** - основной движок плеера
3. **`effects.css`** - визуальные эффекты и анимации
4. **`ep1.json`** - пример структуры данных истории

## 🚀 Инновационные особенности

### 🧠 Умная система эффектов:
- **Контекстное понимание** - анализирует эмоции в тексте
- **Прогрессивная интенсивность** - "жжет" → "обжигает" → "раскален" = усиление эффекта
- **Комбо-активация** - несколько ключевых слов = мощный визуальный эффект

### 📱 TikTok-готовый контент:
- Автоматическая запись экрана
- Оптимизация под мобильные устройства
- Кинематографические эффекты в реальном времени

### 🎭 Интерактивная кинематография:
- Читатель = режиссер своего фильма
- Каждое прочтение уникально
- Эмоциональное погружение через синхронизацию текста и визуала

## 💡 Принцип работы триггеров

```
Текст: "Заколка начинает пульсировать и жжет ладонь"
    ↓
Анализ: найдены ключевые слова ["заколка", "пульсировать", "жжет"]
    ↓
Категории: [artifact, fire]
    ↓
Эффекты: [star_shine, fire_glow] с интенсивностью 2
    ↓
Визуал: звездное сияние в центре + красное свечение экрана
```

Это революционный подход к интерактивному сторителлингу! 🎬✨